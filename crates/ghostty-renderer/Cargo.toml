[package]
name = "ghostty-renderer"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Safe Rust wrapper for libghostty-renderer"

[dependencies]
ghostty-renderer-sys = { path = "../ghostty-renderer-sys" }
log.workspace = true

[package]
name = "ghostty-renderer-sys"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Raw FFI bindings to libghostty-renderer"
links = "ghostty-renderer"

[build-dependencies]
cc = "1"

[dependencies]
libc.workspace = true

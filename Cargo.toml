[package]
name = "mmterm"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
description = "A libghostty-based terminal emulator with native multiplexing"
license = "MIT OR Apache-2.0"

[dependencies]

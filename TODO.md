# TODO

- layout can break when resizing
- padding is not working
- cmd key actions dont work
- kitty image protocol: direct + unicode-placeholder placements land; aspect-ratio centering inside the placement grid is naive (chafa/timg-fitted grids work)
- copy stuff doesnt work and needs to be compat with tmux etc.
- cursor is block mode (ghostty its not, maybe config?)
- theme still looks off
- check what we can offload to external libs (e.g. pty impl)
